<h2>Welcome to Litter//Splitter, <%= @user.name %>!</h2>
<br>
<h4>Here's who is currently available for adoption: </h4><br>
<div class="all-cat-grid">
  <% @cats.each do |cat| %>
      <% if !cat.adopted? %>
      <div class="cat-card">
        <img  src="<%= cat.img_url %>">
        <%=link_to cat.name, cat_path(cat)%>
      </div>
      <% end %>
  <% end %>
</div>
